import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const newRooms = ['Mix 1', 'Mix 2', 'Mix 3', 'Mix 6'].map(name => ({ room_name: name, department_id: '144ebd0c-f766-4ea9-8b5f-860031feeda9', is_active: true })); const { data, error } = await supabase.from('rooms').insert(newRooms).select(); console.log(error || data); } run();
