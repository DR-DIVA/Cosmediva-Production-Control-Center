import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { await supabase.from('production_logs').update({ tank_end: 2, piece_quantity: null }).eq('id', '11febe7f-e3cc-4e73-a394-1b384e0b47c8'); console.log('Fixed POF task'); } run();
