import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function clearData() { console.log('Clearing logs...'); const { error: e1 } = await supabase.from('production_logs').delete().not('id', 'is', null); console.log(e1); console.log('Clearing lots...'); const { error: e2 } = await supabase.from('production_lots').delete().not('id', 'is', null); console.log(e2); console.log('Done'); } clearData();  
