import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('production_logs').select('id, status, tank_start, tank_end, processes(process_name), rooms(room_name), production_lots(lot_no, sku_id)').in('status', ['WAITING', 'IN_PROGRESS']); console.log(JSON.stringify(data.filter(t => (t.processes?.process_name || '').toLowerCase().includes('qc') || (t.rooms?.room_name || '').toLowerCase().includes('qc') || (t.processes?.process_name || '').toLowerCase().includes('ตรวจสอบ')), null, 2)); } run();
