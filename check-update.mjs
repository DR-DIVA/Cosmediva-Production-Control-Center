import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from('production_lots').update({ qc_fg_passed_carton_ranges: '1-9' }).eq('id', 'd42a17c3-efba-4792-a522-2b4e2ed8f494'); console.log({data, error}); } run();
