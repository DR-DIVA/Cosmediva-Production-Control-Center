import { createClient } from '@supabase/supabase-js'; const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { console.log(await supabase.from('processes').select('*').ilike('process_name', '%%º ¡%%')); } run();  
