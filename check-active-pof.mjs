import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('production_logs').select('id, status, piece_quantity, processes!inner(process_name)').in('status', ['WAITING', 'IN_PROGRESS']).like('processes.process_name', '%อุโมงค์%'); console.log(data); } run();
