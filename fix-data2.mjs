import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { await supabase.from('production_logs').delete().eq('id', '20f6d70e-85cb-4d67-87eb-40c6791c5c31'); await supabase.from('production_logs').update({ status: 'IN_PROGRESS', piece_quantity: null, end_time: null }).eq('id', '11febe7f-e3cc-4e73-a394-1b384e0b47c8'); console.log('Fixed POF task back to IN_PROGRESS'); } run();
