import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('production_logs').select('*, processes!inner(process_name)').eq('production_lot_id', 'd42a17c3-efba-4792-a522-2b4e2ed8f494').ilike('processes.process_name', '%Õÿ‚¡ß§Ï%').neq('status', 'COMPLETED').order('tank_start'); console.log(JSON.stringify(data, null, 2)); } run();
