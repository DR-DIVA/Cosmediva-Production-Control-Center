import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const p = await supabase.from('processes').select('*').limit(1); const pl = await supabase.from('production_lots').select('*').limit(1); const plog = await supabase.from('production_logs').select('*').limit(1); console.log('Processes:', p.data); console.log('Lots:', pl.data); console.log('Logs:', plog.data); } run();
