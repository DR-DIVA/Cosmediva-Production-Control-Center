const fs = require('fs'); const path = require('path'); const logPath = path.join('C:', 'Users', 'hp', '.gemini', 'antigravity', 'brain', '7f1a3657-7885-4f67-83bd-af128ac1d767', '.system_generated', 'logs', 'transcript_full.jsonl'); const lines = fs.readFileSync(logPath, 'utf8').split('\n'); let fullContent = ''; for(let i = lines.length - 1; i ; i--) { if (!lines[i]) continue; const obj = JSON.parse(lines[i]); if (obj.tool_calls 
