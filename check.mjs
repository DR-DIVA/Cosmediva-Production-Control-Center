import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function checkData() { const { data: logs, error: e1 } = await supabase.from('production_logs').select('*'); console.log('Logs count:', logs?.length, e1); const { data: lots, error: e2 } = await supabase.from('production_lots').select('*'); console.log('Lots count:', lots?.length, e2); } checkData();  
